pl:
  pawsko:
    toolroom:
      database: mysql

spring:
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  datasource:
    url: jdbc:mysql://localhost:3306/tools
    username: root
    password: coderslab
  security:
    user:
      name: admin
      password: admin
  data:
    mongodb:
      host: localhost
      port: 27017
      database: ToolRoom
      repositories:
        type: auto
springdoc:
  swagger-ui:
    default-models-expand-depth: -1